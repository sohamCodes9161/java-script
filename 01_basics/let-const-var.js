let name="soham"
const sex ="male"
var age=19
console.log(name);
console.log(sex,age);
let aspirations;

console.table([name,sex,age,aspirations])
//alert("hello soham") can't use alert in node.js .can be used in browsers only



/*Reassignability: let and var can be reassigned, but const cannot.
Scope: var is function-scoped, while let and const are block-scoped.
Hoisting: All three are hoisted, but let and const are not initialized 
until their declaration line, causing errors if accessed before that.*/


